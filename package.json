{
  "name": "orbit-cli",
  "version": "2.0.0",
  "description": "Orbit CLI — search people from the command line. Thin client for the Orbit MCP server.",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "orbit": "./bin/orbit"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "keywords": [
    "orbit",
    "search",
    "people",
    "cli",
    "mcp"
  ],
  "license": "MIT",
  "dependencies": {
    "@clack/prompts": "^1.1.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
