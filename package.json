{
  "name": "orbit-cli",
  "version": "2.1.0",
  "description": "Orbit CLI — search for anyone from the command line",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "orbit": "./bin/orbit"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "keywords": [
    "orbit",
    "search",
    "people",
    "cli"
  ],
  "license": "MIT",
  "dependencies": {
    "@clack/prompts": "^0.10.0",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
